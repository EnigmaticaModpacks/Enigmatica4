
[Commands]

	[Commands.delhome]
		#Range: > 0
		delay = 0
		aliases = ["delhome"]
		perm = "serverutils.delhome"
		enable = true
		#Range: > 0
		cooldown = 0

	[Commands.delwarp]
		#Range: > 0
		delay = 0
		aliases = ["delwarp"]
		perm = "serverutils.delwarp"
		enable = true
		#Range: > 0
		cooldown = 0

	[Commands.back]
		#Range: > 0
		delay = 5
		aliases = ["back", "return"]
		perm = "serverutils.back"
		enable = true
		#Range: > 0
		cooldown = 30
		enable-on-death = true

	[Commands.endchest]
		#Range: > 0
		delay = 0
		aliases = ["endchest", "ec"]
		perm = "serverutils.endchest"
		enable = true
		#Range: > 0
		cooldown = 0

	[Commands.bomb]
		#Range: > 0
		delay = 0
		aliases = ["bomb", "boom"]
		perm = "serverutils.bomb"
		enable = true
		#Range: > 0
		cooldown = 0

	[Commands.flood]
		#Range: > 0
		delay = 0
		aliases = ["drown"]
		perm = "serverutils.flood"
		enable = true
		#Range: > 0
		cooldown = 0

	[Commands.home]
		#Range: > 0
		delay = 5
		aliases = ["home"]
		perm = "serverutils.home"
		home-max-perm = "serverutils.maxhome_"
		enable = true
		#Range: > 0
		cooldown = 0

	[Commands.feed]
		#Range: > 0
		delay = 0
		aliases = ["feed"]
		perm = "serverutils.feed"
		enable = true
		#Range: > 0
		cooldown = 0
		other-perm = "serverutils.feed.other"

	[Commands.fly]
		#Range: > 0
		delay = 0
		aliases = ["fly"]
		perm = "serverutils.fly"
		enable = true
		#Range: > 0
		cooldown = 0
		other-perm = "serverutils.fly.other"

	[Commands.Ranks]

		[Commands.Ranks.Default]
			prefix = "&8[&7Player&8]"
			perms = ["serverutils.back", "serverutils.delhome", "serverutils.home", "serverutils.maxhome_1", "serverutils.homes", "serverutils.rtp", "serverutils.rtpnear", "serverutils.rtpfar", "serverutils.sethome", "serverutils.spawn", "serverutils.tpa", "serverutils.tpa.here", "serverutils.tpa.deny", "serverutils.tpa.accept", "serverutils.warp", "serverutils.warps"]

	[Commands.kit]
		aliases = ["kit"]
		perm = "serverutils.kit"
		#Range: > 0
		delay = 0
		enable = true
		"modfiy perm" = "serverutils.kit.modify"
		#Range: > 0
		cooldown = 0

		[Commands.kit.rtpnear]
			#Range: > 0
			delay = 5
			aliases = ["rtpnear"]
			perm = "serverutils.rtpnear"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.repair]
			#Range: > 0
			delay = 0
			aliases = ["repair"]
			perm = "serverutils.repair"
			enable = true
			#Range: > 0
			cooldown = 5
			"repair all perm" = "serverutils.repair.all"

		[Commands.kit.tpaAccept]
			#Range: > 0
			delay = 0
			aliases = ["tpaccept"]
			perm = "serverutils.tpa.accept"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.tpa]
			#Range: > 0
			delay = 0
			aliases = ["call"]
			perm = "serverutils.tpa"
			enable = true
			#Range: > 0
			cooldown = 0
			#Range: > 0
			"tp time" = 5

		[Commands.kit.smite]
			#Range: > 0
			delay = 0
			aliases = ["smite"]
			perm = "serverutils.simte"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.permission]
			#Range: > 0
			delay = 0
			aliases = ["permission", "perms", "p"]
			perm = "serverutils.permission"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.setwarp]
			#Range: > 0
			delay = 0
			aliases = ["setwarp"]
			perm = "serverutils.setwarp"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.setspawn]
			#Range: > 0
			delay = 0
			aliases = ["setspawn"]
			perm = "serverutils.setspawn"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.rtp]
			#Range: > 0
			delay = 5
			aliases = ["rtp"]
			perm = "serverutils.rtp"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.nick]
			#Range: > 0
			delay = 0
			aliases = ["nickname", "nick"]
			perm = "serverutils.nick"
			enable = true
			#Range: > 0
			cooldown = 0
			other-perm = "serverutils.ck.other"

		[Commands.kit.warps]
			#Range: > 0
			delay = 0
			aliases = ["warps"]
			perm = "serverutils.warps"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.spawn]
			#Range: > 0
			delay = 5
			aliases = ["spawn"]
			perm = "serverutils.spawn"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.rtpfar]
			#Range: > 0
			delay = 5
			aliases = ["rtpfar"]
			perm = "serverutils.rtpfar"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.sethome]
			#Range: > 0
			delay = 0
			aliases = ["sethome"]
			perm = "serverutils.sethome"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.rank]
			modify-perm = "serverutils.rank.modify"
			#Range: > 0
			delay = 0
			aliases = ["rank"]
			perm = "serverutils.rank"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.tpaDeny]
			#Range: > 0
			delay = 0
			aliases = ["tpdeny"]
			perm = "serverutils.tpa.deny"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.tpaHere]
			#Range: > 0
			delay = 0
			aliases = ["tpahere"]
			perm = "serverutils.tpa.here"
			enable = true
			#Range: > 0
			cooldown = 0

		[Commands.kit.warp]
			#Range: > 0
			delay = 5
			aliases = ["warp"]
			perm = "serverutils.warp"
			enable = true
			#Range: > 0
			cooldown = 0

	[Commands.heal]
		#Range: > 0
		delay = 0
		aliases = ["heal"]
		perm = "serverutils.heal"
		enable = true
		#Range: > 0
		cooldown = 0
		other-perm = "serverutils.heal.other"

	[Commands.homes]
		#Range: > 0
		delay = 0
		aliases = ["homes"]
		perm = "serverutils.homes"
		enable = true
		#Range: > 0
		cooldown = 0

	[Commands.god]
		#Range: > 0
		delay = 0
		aliases = ["god", "tgm"]
		perm = "serverutils.god"
		enable = true
		#Range: > 0
		cooldown = 0
		other-perm = "serverutils.god.other"

[Features]

	[Features."Color Chat"]
		perm = "serverutils.colorchat"
		enabled = true

		[Features."Color Chat"."Color Codes"]
			#This will enable the &h color code, which will change text based on the month it is.
			"holiday enabled" = true
			#This will enable the &g color code, which will change text to rainbow.
			"rainbow enabled" = true

			[Features."Color Chat"."Color Codes"."Holiday Colors"]
				June = ["GRAY", "WHITE", "RED"]
				October = ["DARK_GRAY", "GOLD", "WHITE"]
				December = ["GREEN", "WHITE", "RED"]
				May = ["GREEN", "YELLOW", "AQUA", "BLUE"]
				September = ["DARK_GRAY", "RED", "YELLOW"]
				March = ["DARK_GREEN", "GREEN", "WHITE", "GOLD"]
				July = ["RED", "WHITE", "BLUE"]
				January = ["DARK_GRAY", "GRAY", "GOLD", "WHITE"]
				April = ["AQUA", "GREEN", "BLUE", "WHITE"]
				Febuary = ["WHITE", "LIGHT_PURPLE", "DARK_PURPLE", "RED"]
				August = ["GOLD", "RED", "GREEN"]
				November = ["RED", "GOLD", "YELLOW"]

	[Features."Auto Save"]
		#How many minutes until the ServerData will be saved. 0 disables this
		#Range: > 0
		delay = 5

